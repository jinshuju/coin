import React from "react";
import styled from "styled-components";

const Header = styled.header`
  display: inline-block;
  margin-left: 50px;
`;

export default () => (
  <Header>
    <h1>Beautiful Icons(双击图标即可复制名称）</h1>
  </Header>
);